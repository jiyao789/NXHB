@import "../../styles/utilities.wxss";
@import "../../styles/icons.wxss";

/* 微信小程序 button 默认 line-height 会阻止 flex 布局生效，需重置 */
button {
  display: flex;
  align-items: center;
  justify-content: center;
  line-height: 1;
  padding: 0;
  margin: 0;
}

button::after {
  border: none;
}
